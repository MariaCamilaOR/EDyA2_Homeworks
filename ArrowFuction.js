// Arrow Function
  
const verificarParidad_AF = (numero) => { return (numero % 2 === 0) ? "par" : "impar";};

export { verificarParidad_AF };

  /*
  // Solicita al usuario que ingrese un número
  const numeroIngresado = prompt("Ingresa un número:");
  // Convierte el valor ingresado a un número y llama a la función
  verificarParidad_AF(Number(numeroIngresado));
  */
